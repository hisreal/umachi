<?php

declare(strict_types=1);

return [
    // Retention is intentionally advisory. Logs are never deleted automatically.
    'retention_months' => 12,
    'default_per_page' => 20,
    'max_per_page' => 100,
];
